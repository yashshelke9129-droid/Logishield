# LogiShield API runtime dependencies.
#
# These are the exact versions from the working local
# environment (Python 3.13). Only packages imported by the
# running API (backend/main.py + backend/recovery_engine.py)
# are listed here, which keeps Render builds fast and the
# free-tier memory footprint small. The lightweight runtime
# delay model does not need scikit-learn/joblib at request
# time; offline training/generation scripts may install
# scikit-learn, joblib, Faker etc. separately.

fastapi==0.141.1
uvicorn[standard]==0.52.3
SQLAlchemy==2.0.52
psycopg2-binary==2.9.12
pandas==3.0.5
numpy==2.5.2
python-dotenv==1.2.2
